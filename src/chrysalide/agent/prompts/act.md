Tu es dans la phase ACT.
Plan en cours : {plan}
Objectif : {goal}

Implémente le code nécessaire.
Utilise l'outil `fs.write` pour créer ou modifier les fichiers, et `shell.exec` si besoin d'interagir.
N'utilise PAS de `# TODO` ou `pass`. Implémente la solution complète.

Quand tu as terminé d'implémenter les tâches, n'appelle plus d'outils et dis "TERMINE".
